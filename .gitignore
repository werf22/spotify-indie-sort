.venv/
.audio-venv/
.essentia-venv/
__pycache__/
*.pyc
data/
nohup.out
*.log
.env
.DS_Store
.cache.db

# Rust build output — regenerable with `cargo build --release`
onetagger-db/target/
# vendored dependency; its own .git internals must not be tracked
vendor/onetagger/.git/

# Spotlight exclusion marker for the 59 GB data tree (local only)
data/.metadata_never_index

# Built native app (rebuild with native/build.sh)
Similar Tracks.app/
native/engine.log
traktor_drag/Traktor Drag.app/

# Timestamped .env backups — they contain the same secrets as .env itself.
.env.bak-*
.env.bak
